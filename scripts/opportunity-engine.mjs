// Continuous opportunity engine is intentionally disabled at build time.
// The previous source-rewrite implementation is disabled until the engine
// is integrated directly into src/both.jsx without mutating source at build time.
console.log('Opportunity engine build patch disabled; preserving src/both.jsx');
